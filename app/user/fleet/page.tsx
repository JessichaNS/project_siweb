import FleetUserPage from './fleet-user';

export const metadata = {
  title: 'Fleet | Shipy User',
  description: 'View active marine vessels and fuel status.',
};

export default function Page() {
  return <FleetUserPage />;
}